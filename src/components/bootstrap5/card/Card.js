import React from "react";
import axios from "axios";

const Card = () => {
    const uploadFile = (e) =>{
        console.log(e.target.files)
    }
    return (
        
        <div>card</div>
    );
}


export default Card;